#include <Arduino.h>

void setup() {
  // put your setup code here, to run once:
  
  
  // have an interrupt for hermatic seal broken
  

}

void loop() {
  
  // put your main code here, to run repeatedly:
  
  // seal hits
  
  // led goes off! for speaker turns / pump strts (two LEDs, one each)

  // how do we make sure that 
  
  	// we need a t=0, could connect all other peripherals to a digital pin
  	// so they all know t=0 at the same time

  // SAMPLING

    // logically figure out how much time has elapsed

    // decimate in real time (get every 10th value)

    // OR get 10 values and then average

    // interpolation?
  
    // both mics at once

  // AM modulated = A*sin A function of time , gaussian

  // AM is a gaussian

}
